import { ZeroExSwapResponse } from '../types';
export declare function getZeroExSwapQuote({ sellAmount, sellToken, buyTokenAddress, slippagePercentage, networkId, }: {
    sellAmount: string;
    sellToken: string;
    buyTokenAddress: string;
    slippagePercentage?: string;
    networkId: number;
}): Promise<ZeroExSwapResponse>;
export declare function validateSlippage(slippage?: string): void;
